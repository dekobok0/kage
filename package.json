{
    "name": "kage-app",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "concurrently \"pnpm --filter bff-server dev\" \"pnpm --filter kage start\"",
        "make": "pnpm --filter kage build"
    },
    "devDependencies": {
        "@puppeteer/browsers": "^2.10.6",
        "concurrently": "^8.2.2"
    },
   "pnpm": {
    "onlyBuiltDependencies": [
      "electron"
    ]
  }
}
