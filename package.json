{
    "name": "kage-app",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev --workspace=bff-server\" \"npm run start --workspace=kage\"",
        "build": "npm run build:vite --workspace=kage && electron-builder"
    },
    "devDependencies": {
        "@puppeteer/browsers": "^2.10.6",
        "concurrently": "^8.2.2"
    }
}
